# Core: needed for training and evaluation
torch>=2.0
monai>=1.3
einops                 # required by MONAI's SwinUNETR attention blocks
numpy
scipy                  # resampling and connected-component analysis
SimpleITK              # NIfTI / DICOM I/O
nibabel                # MONAI's NIfTI backend
tqdm

# Only needed for --arch nnunet
# dynamic-network-architectures

# Only needed for the visualisation utility (view_scan.py)
# matplotlib

# Install PyTorch matched to your CUDA version first, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# then:
#   pip install -r requirements.txt
